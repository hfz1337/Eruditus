version: '2'

services:
  eruditus:
    build: .
    image: eruditus:latest
    container_name: eruditus
    restart: always
  mongodb:
    image: mongo:latest
    container_name: monogdb
    restart: always
    volumes:
      - .data:/data/db
